{
  "name": "parkpulse-hedera-contracts",
  "version": "1.0.0",
  "description": "Hedera smart contracts for ParkPulse.ai",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy.js --network hedera_testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network hedera_mainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.4.5"
  }
}
